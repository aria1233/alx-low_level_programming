#include "main.h"

/**
 * main - function that tests and prints whether a number is negative or positive
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
