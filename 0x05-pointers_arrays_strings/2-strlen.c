#include "main.h"
#include <stdio.h>

/**
 * int _strlen - returns length of string
 * @s: number being tested
 * Return: always 0
 */

int _strlen(char *s)
{
	char *s;
	int len;

	s = "string";
	
	len = _strlen(char *s);
	printf("_strlen(char *s): %d\n", len);
	return (0);
}
